from api.main import app

# This is necessary for Vercel formatting if using legacy builds, 
# or just as an entry point. 
# Vercel looks for 'app' variable by default in supported frameworks or requires configuration.
